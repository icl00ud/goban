# Goban — Web interface

This is the web interface for Goban. It runs as part of the main server — you don't need to run it separately.

Start the server and open `http://localhost:8080`.

For setup instructions and full documentation, see the [main README](../README.md).
